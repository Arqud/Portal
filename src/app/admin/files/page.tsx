import { ComingSoon } from "@/components/ComingSoon";
export default function FilesPage() {
  return <ComingSoon section="Files" phase={7} bullets={["Upload files with drag-and-drop","Organise by client and category (Contracts, Brand Assets, Reports, Ad Creatives)","Share files with clients — they appear in the client Documents section","Download and delete files"]} />;
}
