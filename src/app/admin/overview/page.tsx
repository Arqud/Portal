import { ComingSoon } from "@/components/ComingSoon";
export default function OverviewPage() {
  return <ComingSoon section="Overview" phase={3} bullets={["Active client count and live campaign count","Total leads and revenue this month across all clients","Campaign performance chart — leads over time","Recent invoices with payment status"]} />;
}
