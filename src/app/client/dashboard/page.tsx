import { ComingSoon } from "@/components/ComingSoon";
export default function ClientDashboardPage() {
  return <ComingSoon section="Dashboard" phase={4} bullets={["KPI cards: Leads This Month, Cost Per Lead, Ad Spend, Reach","Campaign breakdown with per-branch performance","Latest invoice widget with payment status","Latest report widget with download link"]} />;
}
