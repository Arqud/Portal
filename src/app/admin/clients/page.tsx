import { ComingSoon } from "@/components/ComingSoon";
export default function ClientsPage() {
  return <ComingSoon section="Clients" phase={3} bullets={["List all clients with Active / Inactive status","Add a new client and generate their portal login","Edit client details and subdomain slug","View the portal as any client would see it"]} />;
}
