import { ComingSoon } from "@/components/ComingSoon";
export default function ClientInvoicesPage() {
  return <ComingSoon section="Invoices" phase={4} bullets={["Full invoice history with status (Pending / Paid / Overdue)","Download each invoice as a branded PDF","Filter by date range"]} />;
}
