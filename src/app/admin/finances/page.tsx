import { ComingSoon } from "@/components/ComingSoon";
export default function FinancesPage() {
  return <ComingSoon section="Finances" phase={6} bullets={["Create invoices with line items, VAT, and ARQUD branding","Track Pending / Paid / Overdue status per invoice","Download invoices as branded PDFs","Revenue summary: this month, last month, year to date"]} />;
}
