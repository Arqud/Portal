import { ComingSoon } from "@/components/ComingSoon";
export default function CampaignsPage() {
  return <ComingSoon section="Campaigns" phase={5} bullets={["All Meta Ads campaigns across all clients","Per-campaign: Leads, Cost Per Lead, Spend, Reach, CTR","Filter by client, auto-sync from Meta Marketing API","Manual refresh button"]} />;
}
