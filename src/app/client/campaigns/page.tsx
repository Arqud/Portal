import { ComingSoon } from "@/components/ComingSoon";
export default function ClientCampaignsPage() {
  return <ComingSoon section="Campaigns" phase={4} bullets={["All your active campaigns with live metrics","Per-campaign: Leads, CPL, Spend, Reach, CTR","Performance chart showing leads over time","Branch / location breakdown"]} />;
}
