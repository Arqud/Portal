export default function Home() {
  return (
    <main className="min-h-screen px-8 py-16">
      <h1 className="text-5xl tracking-wide">ARQUD Portal</h1>
      <p className="mt-4 max-w-xl text-arqud-bone">
        Dark gold theme probe — heading should be gold serif, body should be
        bone sans-serif on near-black background.
      </p>
      <p className="mt-6 text-sm text-arqud-muted">Phase 1 — Scaffold</p>
    </main>
  );
}
