import { ComingSoon } from "@/components/ComingSoon";
export default function ClientReportsPage() {
  return <ComingSoon section="Reports" phase={4} bullets={["Monthly performance reports uploaded by your agency","Each report: period, date added, PDF download"]} />;
}
